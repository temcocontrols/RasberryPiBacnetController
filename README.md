Starting from the official Raspbian Jessie image install QT:

apt-get install libqt5all

Inside the code do a:

qmake

Then:

make

That'll generate QRasberryPiBacnetController that cou can run like ./QRasberryPiBacnetController
